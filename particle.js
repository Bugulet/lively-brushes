class Particle {
    constructor() {
        this.velocity = p5.createVector(0, 0);
        this.position = p5.createVector(0, 0);
        this.mousePosition = p5.createVector(mouseX, mouseY);
    }

    update(orbitPointX, orbitPointY) {

    }

    show() {

    }
}